/**
 * Supabase Auth - Node.js-only CLI authentication
 * Uses node:http (local server) and the `open` package to handle OAuth.
 * Never import this from browser/Next.js client code.
 */

export { SupabaseAuth } from '../supabase/auth.js';
